// 단위전략1
template<typename T> class vector
{
public:
	void resize(int size)
	{
		// 메모리가 부족합니다.
		// 메모리 재할당을 해야 합니다.
		// 어떻게 할까요 ?
		// malloc, calloc, new, linux(window)system call ?
	}
};